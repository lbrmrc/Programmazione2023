#include <stdio.h>
main(){
    // definisce una variabile intera di nome v;
    int v;
    int doppio;

    // richiede all'utente il valore di v
    scanf("%d", &v);
    doppio = v*2;

    // stampa il doppio di v;
    printf("%d\n", doppio);
}